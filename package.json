{
  "name": "smart-sciences",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACK,FRONT -c cyan,magenta \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend/app && ../venv/Scripts/uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "frontend": "cd frontend/Smart-Sciences && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
